# Example only. Copy to `.env` and fill real values.
# Do NOT commit `.env` (it is gitignored).

CLIPPER_USERNAME=
CLIPPER_PASSWORD=

# Only needed when QC and Wiki use different accounts:
CLIPPER_QC_USERNAME=
CLIPPER_QC_PASSWORD=
CLIPPER_WIKI_USERNAME=
CLIPPER_WIKI_PASSWORD=
